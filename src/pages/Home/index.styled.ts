import styled from "styled-components";

export const DivApp = styled.div`
    text-align: center;

    .app-logo {
        height: 40vmin;
        pointer-events: none;
    }

    .button{flex-basis: 200px}
`;
